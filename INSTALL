Locale::Identifier Installation

# install via cpanminus
cpanm Locale::Identifier

# install via CPAN shell
cpan Locale::Identifier

# install via ActivePerl
ppm install Locale::Identifier

# install manually
perl Build.PL
./Build
./Build test
./Build install
